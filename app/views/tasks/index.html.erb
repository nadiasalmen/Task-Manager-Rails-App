<div class="container">
  <h1 class="my-3 font-weight-bolder text-info">My Tasks</h1>
  <% @tasks.each do |task| %>
    <p class="mb-0 text-secondary">
      <% if task.completed == true %>
        <i class="far fa-check-square"></i>
      <% else %>
        <i class="far fa-square"></i>
      <% end %>
      <%= task.title.capitalize %>
    </p>
  <% end %>
</div>

